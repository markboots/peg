#include "PESolver.h"

PESolver::PESolver(const PEGrating& grating, const PEMathOptions& mo) {
	// set math options.
	
	// allocate matrices and vectors
	
	
}

/// \todo Imp.
PESolver::~PESolver() {
	
}

/// \todo Imp.
PEResult PESolver::getEff(double incidenceDeg, double wl) const {
	
	// get refractive index at wavelength
	
	// compute all alpha_n and beta_n.
	
	
	// for all p
		// insert initial values
		// integrate over y
			// compute M(y)
		// get u(a) and u'(a)
	
	// construct Ax = b matrices
	// solve for An
	// construct Ax = b matrices
	// solve for Bn
	
	return PEResult();	
}
